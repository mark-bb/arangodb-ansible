- name: Start {{ adb_service_name }} service
  ansible.builtin.systemd_service:
    name: "{{ adb_service_name }}"
    state: "started"
    
- name: Waiting for ArangoDB Starter
  ansible.builtin.wait_for:
    port: 8528
    delay: 5
    timeout: 60
    msg: "ArangoDB Starter failed to start"
    
